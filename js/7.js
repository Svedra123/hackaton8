// pulgadas a pedir con metros m/p

const metros = Number(prompt("Ingrese los metros a pedir"))

const pulgada = 0.0254

let pulped

pulped = metros / pulgada

alert("Las pulgadas a pedir son: " + pulped)